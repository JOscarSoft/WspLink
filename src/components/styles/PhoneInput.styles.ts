import { StyleSheet } from 'react-native';

export const phoneInputStyles = StyleSheet.create({
    input: {
        padding: 16,
        borderRadius: 12,
        borderWidth: 1,
        fontSize: 16,
        marginBottom: 16,
    },
});
